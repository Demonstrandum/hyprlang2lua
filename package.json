{
  "name": "hyprlang2lua",
  "version": "0.1.0",
  "description": "Converts legacy Hyprland hyprlang (.conf) configs to the Lua config format",
  "type": "module",
  "bin": { "hyprlang2lua": "./src/cli.ts" },
  "scripts": {
    "extract": "bun run tools/extract-schema.ts",
    "test": "bun test",
    "convert": "bun run src/cli.ts"
  },
  "devDependencies": { "@types/bun": "latest" }
}
